SELECT s.Name FROM STUDENTS AS s INNER JOIN FRIENDS AS f ON s.ID = f.ID INNER JOIN PACKAGES AS ps ON s.ID = ps.ID INNER JOIN PACKAGES AS pf ON f.Friend_ID = pf.ID WHERE ps.Salary < pf.Salary ORDER BY pf.Salary
